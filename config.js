export const CONFIG = {
    canvasWidth: window.innerWidth,
    canvasHeight: window.innerHeight,
    backgroundColor: '#222',
    entityCount: 15,
    entityMinSize: 20,
    entityMaxSize: 80,
    growthSpeed: 0.3,
    shrinkSpeed: 0.5,
    maxBlobsPerEntity: 5
};
